---
id: task-15-sync-cli-help-text
priority: low
status: done
description: |
  Ensure the `HELP_TEXT` in `src/ralph_loop/cli.py` is synchronized with the current implementation and options.
  
  Plan:
  1. Audit `build_parser` in `src/ralph_loop/cli.py`.
  2. Audit `build_options` in `src/ralph_loop/commands.py`.
  3. Update `HELP_TEXT` if any discrepancy is found.
  4. Run tests.
---
